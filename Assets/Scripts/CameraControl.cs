using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CameraControl : MonoBehaviour
{

  private void Start()
  {
    //WebCamTexture webcamTexture = new WebCamTexture();
    //rawImage.texture = webcamTexture;
    //rawImage.material.mainTexture = webcamTexture;
    //webcamTexture.Play();
  }

  // Update is called once per frame
  void Update()
  {
    //bool shift = Input.GetKey(KeyCode.LeftShift) || Input.GetKey(KeyCode.RightShift);

    //if (Input.GetKeyDown(KeyCode.UpArrow))
    //{
    //  player.transform.rotation *= Quaternion.Euler(new Vector3(-90, 0, 0));
    //} else if (Input.GetKeyDown(KeyCode.DownArrow))
    //{
    //  player.transform.rotation *= Quaternion.Euler(new Vector3(90, 0, 0));
    //} else if (Input.GetKeyDown(KeyCode.LeftArrow))
    //{
    //  player.transform.rotation *= Quaternion.Euler(new Vector3(0, -90, 0));
    //} else if (Input.GetKeyDown(KeyCode.RightArrow))
    //{
    //  player.transform.rotation *= Quaternion.Euler(new Vector3(0, 90, 0));
    //}
    //else if (!shift && Input.GetKeyDown(KeyCode.UpArrow))
    //{
    //  camera_main.transform.Rotate(new Vector3(0, 0, 90));
    //} else if (!shift && Input.GetKeyDown(KeyCode.DownArrow))
    //{
    //  camera_main.transform.Rotate(new Vector3(0, 0, -90));
    //}

  }
}
